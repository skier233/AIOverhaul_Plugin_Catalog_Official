name: example_recommender_2
human_name: Example Recommender 2
server_link: https://github.com/example/example_recommender_2
version: 0.1.0
required_backend: '>=0.0.0'
files:
  - recommender
# example of depending on another plugin. Useful for recommenders that need data
# stored in the db that was generated by another plugin.
depends_on: ["skier_aitagging_plugin"]
